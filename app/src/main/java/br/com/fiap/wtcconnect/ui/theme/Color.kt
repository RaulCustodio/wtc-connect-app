package br.com.fiap.wtcconnect.ui.theme

import androidx.compose.material3.lightColorScheme
import androidx.compose.ui.graphics.Color

// Paleta de cores inspirada na identidade visual da WTC
val RoyalBlue = Color(0xFF0033A0) // Azul principal
val LightGray = Color(0xFFF0F2F5) // Fundo e elementos secundários
val DarkGray = Color(0xFF657786)  // Textos secundários
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF14171A)
val AccentGreen = Color(0xFF17BF63) // Para status online, notificações de sucesso
val AccentOrange = Color(0xFFF45D22) // Para alertas, campanhas
val AccentBlue = Color(0xFF1DA1F2)   // Para links, ações primárias


// In Color.kt
private val DarkColorScheme = lightColorScheme(
    // Define your dark theme colors here later
    // For now, you can just use the light theme colors as a placeholder
    primary = RoyalBlue,
    // ... etc
)