services:
  mongo:
    image: mongo:7
    container_name: wtc-connect-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: wtc_connect
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: